# Agencia de Viajes — variables de entorno
# Copiar a `.env` para desarrollo local: `cp .env.example .env`

# Supabase: obligatorias para desarrollo/producción.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.<...resto del JWT...>

# Activar únicamente después de crear al menos un usuario administrador
# y aplicar la migración 20260810010000_authenticated_access.sql.
VITE_REQUIRE_AUTH=false
